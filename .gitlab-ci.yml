image: "node:latest"

cache:
  key: "$CI_JOB_NAME-$CI_COMMIT_REF_SLUG"

before_script:
  - npm ci

stages:
  - test

lint:
  stage: test
  script:
    - npm run lint

test:
  stage: test
  script:
    - npm run test
